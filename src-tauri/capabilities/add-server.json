{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "add-server",
  "description": "Capability for the add-server window",
  "windows": ["add-server"],
  "permissions": ["clipboard-manager:allow-read-text"]
}
